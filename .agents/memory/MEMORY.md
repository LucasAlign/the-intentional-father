- [replit-auth-web lib setup](replit-auth-web-lib.md) — new web auth lib needs composite:true + ambient import.meta.env d.ts or typecheck:libs fails.
- **Commit/push policy (split-gate, set 2026-08-25 per jason.e.vanluvanee@gmail.com):** Auto-commit and auto-push freely to feature/working branches (e.g. `claude/adoring-ride-ck1wjw`) as durable checkpoints — no approval needed. This session runs in an ephemeral container; only pushed commits survive inactivity/reclamation, so pushing to the branch is the safety net, not a risk. Never touch `main` without asking first: no merging into `main`, no opening/updating a PR intended for review, no deploying, and no destructive git op (force-push, rebase, reset --hard) without explicit go-ahead. This reconciles wanting to approve major changes with not wanting work lost if the user steps away mid-session.
